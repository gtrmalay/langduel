# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/langduel?sslmode=disable

# JWT Secret (change in production!)
JWT_SECRET=change_me_to_random_string

# AI Configuration (OpenRouter) - optional, falls back to default phrases
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemma-3-4b-it:free
