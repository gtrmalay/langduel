# LangDuel Nginx Configuration for Ihor.cloud
# Place this in /etc/nginx/sites-available/langduel

server {
    listen 80;
    server_name your-domain.com;
    
    # Redirect to HTTPS (enable after SSL setup)
    # return 301 https://$server_name$request_uri;
}

# HTTPS configuration (uncomment after SSL setup)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     
#     # Frontend (SvelteKit build output)
#     location / {
#         root /var/www/langduel/frontend/.output/public;
#         try_files $uri $uri/ /index.html;
#     }
#     
#     # Backend API proxy
#     location /api/ {
#         proxy_pass http://localhost:8080/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_cache_bypass $http_upgrade;
#     }
#     
#     # WebSocket proxy
#     location /ws {
#         proxy_pass http://localhost:8080/ws;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }
